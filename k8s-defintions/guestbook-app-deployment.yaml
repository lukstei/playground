apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
  labels:
    app: guestbook
    name: guestbook-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: guestbook
  template:
    metadata:
      name: guestbook
      labels:
        app: guestbook
        name: guestbook-pod
    spec:
      containers:
        - name: kubernetes/guestbook:v2
          ports:
            - containerPort: 80
